# Services

Services contain business rules, transactions, orchestration, and external service calls.

Controllers should call services instead of manipulating models directly when behavior has more than simple read/write logic.
