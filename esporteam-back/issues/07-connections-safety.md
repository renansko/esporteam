# 07 - Connections And Safety

Criar convites, bloqueios e denuncias.

## Acceptance

- [x] Migration `connections` com convite, amizade, interesse e bloqueio.
- [x] `POST /api/connections` cria convite/interesse/bloqueio.
- [x] `PATCH /api/connections/{id}` aceita ou recusa convite.
- [x] Migration `reports`.
- [x] `POST /api/reports` cria denuncia.
- [x] Bloqueio remove ambos os usuarios da discovery.

## Notes

Seguranca e privacidade sao parte do produto, nao pos-MVP.
