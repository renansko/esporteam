# Features

Use feature folders when a domain area grows beyond a few components.

Suggested shape:

```text
features/
  teams/
    components/
    composables/
    services/
    views/
```

Shared pieces stay in top-level `components`, `composables`, and `services`.
